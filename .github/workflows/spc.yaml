name: spc build
on: push
env:
  ENV: Production
jobs:
   build:
     runs-on: ubuntu-latest
     env:
       owner: akhil
     steps:
       - uses: actions/checkout@v4
       - uses: actions/setup-java@v4
         with:
           distribution: 'temurin'
           java-version: '17'
           cache: maven
       - name: maven build
         run: ./mvnw -B verify
         